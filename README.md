 
  1️⃣ What is the difference between var, let, and const?

  Ans:Difference between var,let,const-
  
  >Var:var is very old version to declare variable. it dosent follow boundaries for this reasone sometimes cause eroors in large programs.
  
  >let:This is modern way to store data.it works specific bounderis.we can change data anytime when we want.

  >Const:we can use const when we want never change the variable.it stand for constant.

  2️⃣ What is the spread operator (...)?
  
  Ans:spread operator means spreading out all the elements inside a list.it is using for easily copy everything one list another list.
  3️⃣ What is the difference between map(), filter(), and forEach()?

  Ans:the difference between map(), filter(), and forEach() describe it-
  >map():we can use it transform into a list and return a new list with those changes.

  >filter():filter conditionally pick some item from list and create a new list.

  >forEach():its loops all item but dosent make any new item list.

  4️⃣ What is an arrow function?
  
  Ans:Arrow function is a function which is to write down short way for funtion.funtion's symbole is (=>)its use for clear code.
  
  5️⃣ What are template literals?
---


git push origin main